interface Config {
    apiUrl: string, /* without / in the end of the string */
}

const config: Config = {
    apiUrl: 'https://api-prawaruchu.mokhnachev.org',
    // apiUrl: 'http://localhost:8080'
}

export default config;
